# Telecode v1.2 Features — Execution Prompt

> Auto-generated from plan: docs/_handoff/v1.2.md
> Stages: 4 | Total deliverables: 11

## TASK
Implement tất cả tính năng v1.2 cho Telecode: file sharing, smart notifications, cost tracking, session templates, scheduled tasks, session search, pinned context, voice-to-prompt, session replay, agent chain, auto-verify.

## WORKING DIR
`/Users/koi/Documents/workspaces/telecode`

## CONTEXT
- Bot Telegram dùng grammyjs, agents (claude/kiro/codex/cursor), SQLite via better-sqlite3
- Session lifecycle: idle → running → waiting_approval → closed
- Existing: attachments.ts (download only), notifier.ts, session/store.ts, conversation-storage.ts
- Commands registry tại src/bot/commands/index.ts
- Schema tại src/session/schema.sql

## CONSTRAINTS
- TypeScript strict, pnpm
- Backward compatible — không break existing sessions/schema (dùng migrations)
- Tất cả commands mới phải register qua commands-registry.ts
- Tests phải pass: `pnpm test`
- Giữ SQLite WAL mode, không đổi sang DB khác

## EXECUTION — 4-STAGE CHAIN

> ⚠️ Đây là orchestrator prompt. Không implement trực tiếp.
> Chạy từng stage tuần tự. Mỗi stage = 1 sub-agent DAG blocking.

---

### Stage 1: Core Data Layer + High-Priority Independent Features
**Sub-agents**: 2 parallel

- Agent 1 (backend): D3 (Cost Tracking), D4 (Session Templates) — Thêm tables `cost_log`, `templates` vào schema, implement /cost command và /template save|run|list commands
- Agent 2 (backend): D2 (Smart Notification & Quiet Hours), D7 (Pinned Context) — Thêm table `chat_settings` columns cho quiet hours, implement /notify command và .telecode/context.md injection + /context edit

**Key files to read**:
- `src/session/schema.sql`
- `src/session/store.ts`
- `src/bot/commands/index.ts`
- `src/bot/notifier.ts`
- `src/config.ts`
- `src/bot/commands-registry.ts`

**Exit gate**:
- [ ] `pnpm build` passes (no TS errors)
- [ ] `pnpm test` passes
- [ ] `grep -r "cost_log\|templates\|quiet_hours" src/session/schema.sql` returns matches
- [ ] `/cost`, `/template`, `/notify`, `/context` commands registered in commands-registry

**Handoff**: Ghi vào `docs/_handoff/stage-1-done.md`:
- Files created/modified (list paths)
- New DB tables/columns added
- New commands registered
- Test count after stage

---

### Stage 2: File I/O + Search Infrastructure
**Reads**: `docs/_handoff/stage-1-done.md`
**Assumes done**:
- cost_log, templates tables exist
- /cost, /template, /notify, /context commands working
- Quiet hours logic in notifier

**Sub-agents**: 2 parallel

- Agent 1 (backend): D1 (File Sharing hai chiều) — Mở rộng attachments.ts: upload file/ảnh từ Telegram → session inbox, agent output file → sendDocument back. Support: image, PDF, CSV, build artifacts
- Agent 2 (backend): D6 (Session Search & History) — Thêm FTS5 virtual table trên sessions transcript, implement /history search "query" và /history last Nd commands

**Key files to read**:
- `src/bot/attachments.ts`
- `src/bot/router.ts`
- `src/session/store.ts`
- `src/session/schema.sql`
- `src/bot/conversation-storage.ts`

**Exit gate**:
- [ ] `pnpm build` passes
- [ ] `pnpm test` passes
- [ ] `grep -r "sendDocument\|sendPhoto" src/bot/attachments.ts` returns matches (outbound file)
- [ ] `grep -r "fts5\|FTS5\|session_fts" src/session/` returns matches
- [ ] `/history` command registered

**Handoff**: Ghi vào `docs/_handoff/stage-2-done.md`:
- Files created/modified
- FTS5 table name and indexed columns
- File sharing: supported MIME types, max size
- Test count after stage

---

### Stage 3: Automation Features
**Reads**: `docs/_handoff/stage-2-done.md`
**Assumes done**:
- File sharing bidirectional working
- FTS5 search on sessions
- All Stage 1 features

**Sub-agents**: 2 parallel

- Agent 1 (backend): D5 (Scheduled Tasks) — Implement /schedule command, cron-like scheduler (node-cron hoặc simple setInterval), auto-create session at scheduled time, gửi kết quả về Telegram
- Agent 2 (backend): D11 (Auto-verify Mode) — Sau session done, tự chạy configured test command, nếu fail → tạo follow-up session với error context, loop max N lần (config), report final result

**Key files to read**:
- `src/session/manager.ts`
- `src/bot/router.ts`
- `src/agents/index.ts`
- `src/config.ts`
- `src/bot/notifier.ts`

**Exit gate**:
- [ ] `pnpm build` passes
- [ ] `pnpm test` passes
- [ ] `grep -r "schedule\|cron" src/` returns matches in new scheduler module
- [ ] `grep -r "auto.verify\|autoVerify" src/` returns matches
- [ ] `/schedule` command registered

**Handoff**: Ghi vào `docs/_handoff/stage-3-done.md`:
- Files created/modified
- Scheduler: storage mechanism, cron syntax supported
- Auto-verify: max retry count, test command config location
- Test count after stage

---

### Stage 4: Advanced Features + Senior Review
**Reads**: `docs/_handoff/stage-3-done.md`
**Assumes done**:
- All Stage 1–3 features working
- Scheduler, auto-verify operational

**Sub-agents**: 2 parallel

- Agent 1 (backend): D8 (Voice-to-Prompt), D9 (Session Replay/Timeline) — Voice message handler via Whisper API (hoặc openai whisper endpoint), transcribe → feed as prompt. Timeline: localhost web endpoint hiển thị tool_log as visual timeline, link gửi qua Telegram
- Agent 2 (backend): D10 (Agent Collaboration/Chain) — Implement /chain command: parse multi-agent pipeline syntax, pipe output session A → input session B, sequential execution with handoff context

**Key files to read**:
- `src/bot/router.ts` (voice message handling)
- `src/bot/tool-render.ts`
- `src/session/manager.ts`
- `src/agents/types.ts`
- `src/agents/registry.ts`
- `src/bot/dashboard.ts`

**Exit gate**:
- [ ] `pnpm build` passes
- [ ] `pnpm test` passes
- [ ] Voice handler registered for voice/audio messages in router
- [ ] `curl http://localhost:*/timeline/*` returns HTML (timeline endpoint)
- [ ] `/chain` command registered
- [ ] Senior review: 0 P0–P1 findings

**Handoff**: Ghi vào `docs/_handoff/stage-4-done.md`:
- Files created/modified
- Voice: API used, fallback behavior when no API key
- Timeline: port config, auth mechanism
- Chain: syntax format, max chain length
- Full feature matrix v1.2 complete

---

## ORCHESTRATOR INSTRUCTIONS

1. Chạy Stage 1 bằng `subagent` tool (blocking mode, role: kiro_default)
2. Sau khi sub-agents return → verify exit gate (chạy test/grep/build trực tiếp)
3. Nếu exit gate PASS → ghi handoff file → proceed Stage 2
4. Nếu exit gate FAIL → fix ngay trong context hiện tại, re-verify, KHÔNG skip
5. Lặp cho đến hết stages
6. Stage cuối: include senior review trong sub-agent prompt (theo workflow §5)
7. Final verify: check ALL exit criteria từ plan document gốc

## ERROR RECOVERY
- Sub-agent fail / partial output → đọc output, identify missing items, re-run stage với scope thu hẹp
- Context approaching limit → ghi progress vào handoff file, báo user resume point
- Test fail sau stage → fix trong stage đó trước khi proceed

## FINAL EXIT CRITERIA
- [ ] All 11 features implemented and accessible via Telegram commands
- [ ] `pnpm build` passes with 0 errors
- [ ] `pnpm test` passes — all new features have test coverage
- [ ] Schema migrations backward compatible (existing data preserved)
- [ ] No regression on existing v1.1 features (session create/run/approve/close still work)
- [ ] All new commands show in /help output
